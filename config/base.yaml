app:
  env: base
  timezone: "UTC"

exchange:
  venue: "binance-futures"
  symbol: "BTCUSDT"

risk:
  max_notional_usd: 10000
  max_leverage: 2
  daily_loss_limit_usd: 200

strategy:
  target_timeframes:
    - "15m"
    - "1h"
  dry_run: true

logging:
  level: "INFO"
  redact_secrets: true
